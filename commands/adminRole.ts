import { Client, Message } from "discord.js";
import GuildController from "../controllers/guildController";

export default class AdminRole {
  static setAdminRole(bot: Client, msg: Message, adminRoleRef: string): void {
    
  }
  static setAdminRoleForce(bot: Client, msg: Message, adminRoleRef: string): void {}
}
